<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Secrets Master Key
    |--------------------------------------------------------------------------
    |
    | 秘密ファイル機能の封筒暗号化（envelope encryption）で使うマスターキー。
    | APP_KEYとは意図的に分離しており、.env漏洩時の影響範囲を切り離す。
    | 32byteのランダム値をbase64（`base64:`プレフィックス付き可）で保持する。
    |
    */

    'master_key' => env('SECRETS_MASTER_KEY'),

    /*
    |--------------------------------------------------------------------------
    | Retention Days
    |--------------------------------------------------------------------------
    |
    | 管理者(admins.id=1)の最終アクティビティからこの日数を超えたら、
    | 秘密ファイルをすべて復元不能な形で抹消する。
    |
    */

    'retention_days' => (int) env('SECRETS_RETENTION_DAYS', 7),

    /*
    |--------------------------------------------------------------------------
    | Chunk Size
    |--------------------------------------------------------------------------
    |
    | 本文の暗号化・ストリーミング復号配信の単位（バイト）。
    | アップロード時のフロントエンドのチャンクサイズとは独立した、暗号化上の単位。
    |
    */

    'chunk_size' => 1024 * 1024,

    /*
    |--------------------------------------------------------------------------
    | Secrets Admin ID
    |--------------------------------------------------------------------------
    |
    | 秘密ファイル機能にアクセスできる唯一の管理者ID。
    |
    */

    'admin_id' => (int) env('SECRETS_ADMIN_ID', 1),
];
